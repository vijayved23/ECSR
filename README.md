# ECSR
Python project to store graphs in E-CSR format. 
- Allows for vertex and edge insertion
- Creating of buffer which allows all edges and vertexes in the buffer to be added at one time
- Allows to search through for edges of certain weights
- Perfecting edge and vertex deletion by testing for edge cases
Shows skills in networkx, pandas, and matplot libraries in python
